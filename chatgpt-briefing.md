# jRelay — External Briefing

Self-contained context for getting a second opinion on this project from another model. Paste this whole file.

## What jRelay is

An Android SMS relay app. A dedicated Android phone hosts a text "group": members text the host's number, and the host re-sends each message to every other member, prefixed with the sender's nickname. There is no server and no app for members — everyone participates over plain SMS, which is the point: many members are on flip phones.

It is vanilla Android Java + XML by deliberate policy. **No Kotlin, no Jetpack, no androidx, no third-party libraries.** UI is `ScrollView` over vertical `LinearLayout`, fully DPAD-navigable, no cards or overlays, built to look right on very small screens. Storage is raw SQLite via `SQLiteOpenHelper`; schema changes are strictly additive (`ALTER TABLE ... ADD COLUMN`) because older bundled SQLite builds can't `DROP COLUMN`.

Upstream is the public repo `sh7411usa/jRelay`. This working copy is a fork of `main` at v4.1, developed locally; the remote here is named `upstream`, and `origin` is intentionally unused so a fork can be attached later.

## The constraint that shapes every decision

Carrier (CTIA) person-to-person guidelines treat roughly 15–60 messages/minute, ~1,000 messages/day, ~100 unique recipients, and a near 1:1 in:out ratio as "human" traffic for one phone number. The live deployment is a neighborhood group of ~100 members on a single consumer line. One post relayed to 100 members is **100 SMS** on that line. Exceeding the guidance risks silent carrier filtering — messages that appear sent but never arrive.

A subtlety worth knowing: jRelay's existing daily limits count **relayed posts, not deliveries**. One post to 100 members counts as 1 against the group limit but costs 100 SMS on the line.

Everything in the roadmap is about reducing sends per post without paying for an A2P/10DLC route.

## Roadmap

`jrelay-handoff.md` at the repo root is the authoritative plan. Phases in brief:

- **Phase 1 → 5.0 (DONE)** — Reply Mode + bare keywords. See below.
- **Phase 2 → 5.1** — Coalescing window: several posts arriving within N seconds merge into one SMS per recipient. Moves salting to send time.
- **Phase 3 → 6.0** — *Respecified mid-session; the doc is authoritative, ignore any older description.* Text-only **group MMS**: members are auto-assigned to sub-groups of at most 9, and each relay becomes one text-only MMS per sub-group via `SmsManager.sendMultimediaMessage` (needs only `SEND_SMS`). 100 members → 12 sends instead of 100. jRelay deliberately stays a **non-default** SMS app; the stock messaging app remains default and downloads everything, and jRelay reads inbound from `content://mms`. No photos, no attachments, no default-SMS-app role. **Gated on a required on-device spike** proving multi-recipient MMS from a non-default app arrives as one group thread, including on a flip phone.
- **Phase 4 → 7.0** — Multiple SIMs on one device, one drain loop per line.
- **Phase 5a → 7.1** — Real sent status via `PendingIntent` (today a send that doesn't throw is assumed delivered).
- **Phase 5b → 8.0** — HTTP failover transport, off by default.
- **Phase 6 (proposed, unscheduled)** — Personal-phone mode via `Prefs.RelayScope`, so the app can run on an everyday phone and relay only explicitly group-addressed messages.

## Open questions worth a second opinion

1. **Is the Phase 3 group-MMS bet sound?** It hinges on multi-recipient `sendMultimediaMessage` succeeding from a non-default SMS app and landing as a single group thread on recipients' phones, including flip phones. If that fails there is no fallback in the plan other than stopping.
2. **Reply Mode's unmetered replies.** Replies deliberately don't count against daily limits, so the quota dashboard under-reports real line usage. The failure mode is carrier filtering while the UI still shows headroom.
3. **Personal-phone mode (Phase 6)** — is "only relay explicitly tagged messages" enough isolation to run this on someone's real phone, or is a dedicated device the only defensible answer?

---

# Run Log

### Run: /build — 2026-09-22
Task:               Phase 1 of the jRelay fork — Reply Mode + bare keywords (v4.1 → 5.0).
Outcome:            APPROVED (Reviewer verdict: APPROVED WITH RECOMMENDATIONS)
Iterations:         2 build iterations + 2 Reviewer cycles, of a 10 cap.
Files:              New `sms/MessageIntent.java` + `MessageIntentTest.java`; modified `CommandProcessor`, `Prefs`, `DbHelper`, `MemberRepository`, `MessageRepository`, `Member`, `MainActivity`, `SettingsActivity`, 3× `strings.xml`, `arrays.xml`, 2 layouts, 1 menu, `build.gradle.kts`, `README.md`, `VERSION.md`.
Key decisions:      Cloned upstream with the remote named `upstream`, leaving `origin` free for a later fork (not under the `yissr` account). Reply target stored as a `message_log` row id (`members.last_post_received_id`) rather than a denormalized author/timestamp pair, so targeting and quota read one source of truth. Routing parser extracted to a pure-Java class with no Android imports specifically so it could be covered by real JVM unit tests — the project already ships plain JUnit, so this cost no new dependency. Bare keywords apply in **all** modes (carrier opt-out convention), which is a deliberate change to Group/Announcement behavior. Reply-target writes batched into one chunked `UPDATE ... id IN (...)` per fan-out instead of one per recipient.
Issues encountered: (1) The initial FILES list missed `res/values/arrays.xml`, which positionally backs the Group Mode spinner — Reply Mode would have been unselectable in Settings. Caught by a Coder reporting a cross-unit gap rather than silently reaching outside its files. (2) **Reviewer caught a real bug that all seven automated checks passed:** the app's own "Send to Group" broadcast and admin DM left `last_post_received_id` stale, so in Reply Mode a member replying to an admin message was silently routed to an unrelated member and the admin never saw it. Fixed by clearing the pointer on both paths, plus on `reactivate`. (3) `/simplify` found that CSV-imported members never received the Reply-Mode `#all` hint that `#add`/`#join` members got — a spec-compliance gap, now fixed via a shared `withReplyHint` helper.
Open items:         No device was available this run, so **all behavioral acceptance is unverified** — every automated check is compile/static/unit-level. The Reviewer's 6-check on-device smoke list (in particular: reply-after-admin-broadcast and reply-after-admin-DM, which exercise the bug that was fixed) must be run before this reaches the live ~100-member group. Also unverified by any automated test: the CSV-import welcome-hint change. Logged as non-blocking for later: surface unmetered reply/`#to` volume in the limits UI; `getRecent` doesn't filter `OUT/REPLY`, so replies appear twice in Recent Activity (pre-existing behavior shared with `#admin`, not a regression).
Config changed mid-run: **`~/.claude/commands/build.md` was edited during this run** at the user's request — `/simplify` now runs after *every* stage that produced production code (once per phase on multi-phase work), before that stage's commit and version bump, with a mandatory Tester re-run afterward. Previously it ran once, at final delivery.

### Run: /build — 2026-09-22 (Phase 2)
Task:               Phase 2 of the jRelay fork — coalescing window (v5.0 → 5.1).
Outcome:            APPROVED (Reviewer verdict: APPROVED WITH RECOMMENDATIONS)
Iterations:         4 Reviewer cycles; 11 agents dispatched in parallel for the build wave, 4 more for `/simplify`.
Files:              New `sms/MessageMerger.java` + tests and `docs/phase3-device-spike.md`; modified `DbHelper`, `OutboxRepository`, `MessageSalt`, `Prefs`, `SmsSendService`, `CommandProcessor`, `SettingsActivity`, `MainActivity`, `activity_settings.xml`, 3× `strings.xml`, `build.gradle.kts`, `README.md`, `VERSION.md`. Suite 35 → 51 tests.
Key decisions:      Merging is **per recipient, not per post** — which is what makes a post folded into an earlier post's undelivered tail work at all. Segment cap measured on a *salt-representative* body, because the zero-width-space salt forces UCS-2 (70 chars/segment instead of 160) and counting the unsalted body would undercount by ~2×. `MessageSalt.applyAll` was re-expressed AS the composition of its two halves rather than mirrored, so the transform order cannot drift. Merge logic isolated in an Android-free class with an injected `SegmentCounter`, purely so it could carry real JVM tests.
Issues encountered: The Reviewer found four things no green test suite could have caught. (1) **Send-rate multiplication** — the hold loop keeps a drain alive while rows are held, so without a guard every inbound post would have added another concurrent burst loop; an active group could have run at many times the configured rate, which is the precise carrier-filtering failure the pacing engine exists to prevent. (2) A **non-transactional merge** racing concurrent drains could double-deliver and clobber a sent row's status. (3) The **segment cap under-counted ~2×** (the ZWSP/UCS-2 issue above). (4) Then **two defects in the guard that fixed (1)** — a suppressed start called `stopSelf` against the most recent id, destroying the service under a live drain (the common case, since coalescing exists for posts that cluster), and the re-entry it recommended used `startService` from a background thread, which throws on API 26+ and would have killed the relay. Re-entry is now in-thread. Separately, moving salting to send time changed behavior even at window=0; the owner decided per-recipient salting is correct (the old behavior sent 100 byte-identical bodies, defeating the point of salting) and it is documented as a deliberate change rather than reverted.
Open items:         **Nothing behavioral is verified** — no device was available, and the lifecycle fixes in particular are invisible to JVM tests. The 8-item on-device checklist below must be run before this touches the live group. Deferred, non-blocking: reset orphaned `SENDING` rows at startup (a mid-drain process kill strands them); wrap the drain in a `catch (Throwable)` so an unexpected DB error logs instead of killing the relay process; a residual few-instruction window where `stopSelf` can target a newly-started drain's id (costs process priority, not messages); one test asserting `applyAll` equals its composition.

### Run: /build — 2026-09-22 (5.2)
Task:               Keep the relay alive — foreground service, reboot recovery, stranded-message repair (v5.1 → 5.2).
Outcome:            APPROVED (Reviewer verdict: APPROVED WITH RECOMMENDATIONS)
Iterations:         2 Reviewer cycles; 6 agents for the build wave, 3 more for `/simplify`.
Files:              New `sms/BootReceiver.java`; modified `SmsSendService`, `NotificationHelper`, `OutboxRepository`, `AndroidManifest.xml`, 3× `strings.xml` (→312), `build.gradle.kts`, `README.md`, `VERSION.md`. No new tests — nothing here is JVM-testable.
Key decisions:      Foreground service chosen as the root-cause fix rather than leaning on battery exemptions, which only paper over it. `specialUse` over `shortService` — the latter caps near 3 minutes and a 100-member fan-out already brushes that, so it would time out mid-group. `startForeground` placed as the first statement of `onStartCommand` on *every* path, including a start about to be suppressed as a duplicate, because `startForegroundService` gives ~5s regardless of what the service then decides. `resetOrphanedSending`'s safety contract documented on the method itself rather than left in its callers.
Issues encountered: (1) **Blocking duplicate-send bug found in review:** `BootReceiver` reset all `SENDING` rows unconditionally. On a Samsung, `BOOT_COMPLETED` lands 30-120s after boot while the radio dumps backlogged SMS earlier — so a fan-out could already be mid-burst, and flipping its claimed rows back to `PENDING` would deliver that burst twice; under ALL_AT_ONCE burst mode, the whole group. Fixed by resetting only on the paused branch. (2) The Reviewer then **corrected its own recommendation**: the `stopSelfResult` fix it asked for narrows the teardown race rather than closing it, and the comment written around it asserted the opposite — reworded, code unchanged. (3) `/simplify` produced a genuine disagreement: two cleanup agents called the `getActiveNotifications()` guard dead code, reasoning that only one drain thread exists; the Reviewer had specifically defended it because a drain *can* outlive its service, and an unguarded `notify()` would then post an ongoing notification with no service behind it. Guard kept. (4) A spec gap of mine: the notification status line was built from hardcoded English until caught by reading the source — the agent's summary described it accurately and still didn't reveal it.
Open items:         **Nothing behavioral is verified** — this phase is almost entirely Android lifecycle, which no JVM test touches. Two items now carry conditions: a **wakelock becomes mandatory** if the screen-off fan-out drifts materially from the configured pacing (an FGS stops the process being killed, not the SoC suspending), and **device check 2 must include a logcat inspection** for `E/SmsSendService: Could not start send service` — the new try/catch means that failure no longer announces itself by crashing. Deferred: notification reports `countPending()` while the dashboard uses `countUnsent()`, so they disagree by the in-flight burst; `ic_dialog_email` is a dialog asset used as a status-bar icon (pre-existing).

### Run: /build — 2026-09-23 (5.3)
Task:               Real delivery results — Phase 5a, brought forward because it was the highest-value work not blocked on the Phase 3 device spike (v5.2 → 5.3).
Outcome:            APPROVED (Reviewer verdict: APPROVED WITH RECOMMENDATIONS)
Iterations:         3 Reviewer cycles; 6 agents for the build wave, 2 for `/simplify`.
Files:              New `sms/SentReceiver.java` + `SentReceiverTest.java`; modified `SmsSendService`, `OutboxRepository`, `DbHelper`, `AndroidManifest.xml`, 3× `strings.xml` (→322), `build.gradle.kts`, `README.md`, `VERSION.md`. Suite 51 → 66.
Key decisions:      The per-attempt token is stored in `handed_off_at` rather than a new column — that column already had to hold a wall-clock timestamp for the staleness sweep, and the token *is* the send's timestamp, so one value serves both and they cannot disagree. PendingIntent identity was moved into the Intent's data Uri rather than the request code, so it derives from persisted values and survives a process restart. The boot-time reset went into `resetOrphanedSending` rather than `BootReceiver` — the Reviewer had framed it as a receiver change and then agreed the repository was correct, because `BootReceiver` only calls the reset when paused, so the common unpaired reboot path would never have reached it.
Issues encountered: **The headline finding was a pre-existing silent data loss.** `sendOne` chose single-vs-multipart with `body.length() > 160`, valid only for GSM-7. Hebrew and Yiddish are UCS-2 at 70 chars/segment, and the ZWSP salt forces UCS-2 on any body — so a 71-160 character message in this group's primary languages was sent as one part, could not be encoded, and never arrived, while being recorded as SENT. Present since the first release; 5.3 is simply the first version able to see it. Beyond that: four of my own spec errors were caught before the build (an unbounded retry in the catch path, a `RESULT_NOT_SENT = -1` sentinel colliding with `Activity.RESULT_OK`, nine strings specced with no renderer, and a `findById` listed in one unit's contract that nobody was assigned to write); the Reviewer found a duplicate-send path through PendingIntent identity across a process restart, a stale-result path where attempt 1's late OK could resolve attempt 2, and an unbounded resend loop in the stale reset; and the fix for the last of those introduced a repeating 10-minute traffic stall, which the Reviewer then also caught.
Open items:         Behavioral acceptance is unverified as always — no device. Two items now carry conditions from earlier phases plus the new checks below. Logged, non-blocking: `hold_until` backoff on retry, longer for `RESULT_ERROR_LIMIT_EXCEEDED` (answering a rate-limit refusal with an immediate resend is the one retry the pacing design would not choose); the unread `lastResult(long)`/`OutboxItem.lastResult`; and an instrumented test source set, without which the reset branches and the token guard depend on manual checks.

### Run: /build — 2026-09-22 (5.4)
Task:               Clear the two non-blocking items 5.3 logged — retry backoff and the unread accessor — plus the `#help` command specced during the 5.0 discussion and never built (v5.3 → 5.4).
Outcome:            APPROVED (Reviewer verdict: APPROVED WITH RECOMMENDATIONS)
Iterations:         1 Reviewer cycle; 3 agents for the build wave, 3 for the post-review fixes, 4 for `/simplify`, 1 Tester.
Files:              Modified `SentReceiver`, `OutboxRepository`, `CommandProcessor`, `SmsSendService` (comment only), 3× `strings.xml` (→326), `SentReceiverTest`, `build.gradle.kts`, `README.md`, `VERSION.md`. Suite 66 → 71.
Key decisions:      Retry backoff reuses the existing `hold_until` column rather than adding a retry-specific one, so it inherits the burst-draw filter, `waitForHold` and the held-row accounting for free — no new column, migration or scheduler. Backoff is per-row, which the Reviewer confirmed is right to ship but is explicitly **not** the end state (see below). `#help` and bare `HELP` deliberately stay distinct: bare `HELP` is a carrier opt-out convention that must keep returning the command list, while `#help` answers "how do I phrase a message right now?" — each reply names the other so neither is a dead end. `/simplify` produced two findings and **both were declined**: collapsing the three `GroupMode` switches into one helper would have replaced three self-checking `case X: return R.string.…_x;` lines with four positionally interchangeable `int` parameters, where a swap compiles silently and ships wrong text — and these strings exist in Hebrew and Yiddish, so a mis-map would be invisible to reviewers and visible only to members.
Issues encountered: The Reviewer's sharpest finding is that **the 5-minute constant is miscalibrated against the problem it targets.** The dominant source of `RESULT_ERROR_LIMIT_EXCEEDED` is Android's own per-app throttle, whose window is 30 messages per 30 *minutes*; with the default retry limit of 1, a row spends its only retry at t+5min while the window is provably still closed, then goes FAILED. On the headline use case — one post to ~100 members — the throttle trips around message 31 and roughly 70 rows each burn their retry into a closed window, every one writing a feed row and potentially tripping admin alerts that are themselves queued onto the same throttled line. Not blocked, because 5.3 resent instantly and failed instantly, so this is strictly better. Separately the Reviewer found that reusing `hold_until` made the dashboard's "Holding *n* to merge" untrue for a host with coalescing switched off — fixed this release along with `#help` giving muted members advice that was false for them. Also corrected: the `retryDelayMillis` javadoc still described the rate refusal as "the carrier explicitly saying to slow down", the exact framing corrected in 5.3's user-facing strings but missed in the code comment.
Open items:         **5.5 is line-level backoff.** `RESULT_ERROR_LIMIT_EXCEEDED` is by construction a property of the line, so backing off one row while the drain keeps feeding the same closed window at full pace means every message the burst dispatches is refused in turn (`sendBurst` has no abort path). The end state is a `Prefs` long plus a check in `drainAll` — no new subsystem — but it changes a loop that has been audited across many rounds, so it earns its own release and test pass. The known miscalibration is recorded at the constant, in `VERSION.md` and in `README.md`. Also deferred: a backed-off RELAY row stays merge-eligible and, keeping its original `enqueued_at`, is usually the row a merge *keeps* — so it hands its spent `attempts` budget to fresh messages that never failed (pre-existing since 5.3, widened here). Untested: nothing exercises `requeueForRetry` → `takeBurst`, i.e. that a backed-off row is genuinely invisible until due and drawable after — the entire point of the change; the five new tests only pin the pure delay function, and the 5-minute value itself is unpinned. **Five releases (5.0–5.4) are now stacked with zero device verification.**

### Run: /build — 2026-09-23 (5.5)
Task:               First remediation wave from the full-codebase audit — the findings that expose data, send duplicates, or take the relay out of the owner's control (v5.4 → 5.5).
Outcome:            APPROVED (Reviewer verdict: APPROVED WITH RECOMMENDATIONS)
Iterations:         1 Reviewer cycle (plus one reviewer that died mid-report and was re-spawned); 7 Coders in parallel, 4 for `/simplify`, 2 Testers.
Files:              `build.gradle.kts`, `AndroidManifest.xml`, both backup XMLs, `OutboxRepository`, `SentReceiver`, `SmsSendService`, `CommandProcessor`, `MemberRepository`, `MemberDetailActivity`, `SettingsActivity`, `RateLimitConfig`, 3× `strings.xml` (→328), `README.md`, `VERSION.md`, `docs/audit-2026-09.md`. Suite unchanged at 71.
Key decisions:      Pacing ceilings are clamped on READ (in `RateLimitConfig`) as well as on save — clamping only at the Settings screen would leave an out-of-range value already stored by an older build in force until someone happened to press Save, which on a dedicated relay phone may be never. The ceilings are documented explicitly as sanity bounds and NOT a rate control, because the inter-burst wait is skipped entirely when staggering is off and `ALL_AT_ONCE` never consults the burst ceiling. The last-admin invariant is enforced at all four call sites rather than at `softRemove`/`setAdmin`; moving it to the data layer would require those methods to report refusal so callers stop sending the "you left the group" notices that follow a successful removal — deferred, with the invariant recorded in javadoc on both chokepoints so a future caller sees it. `/simplify` produced four findings: the duplicate clamp was fixed; a single helper for the six token guards was DECLINED because the guards genuinely differ on `parts_pending`, so a uniform helper would hide the exact difference it was meant to protect.
Issues encountered: **Closing two duplicate-send paths slightly widened the third.** The previously-unguarded `markSent`/`markFailed` would stamp over a stray `resetOrphanedSending` branch-1 reset and accidentally heal that window; correctly guarded, they no longer do. Net strongly positive, but it promotes the branch-1 fix to the next wave rather than a later one. Two agents also caught errors of mine: a brief asserting `"#admin".startsWith("#add")` (false — they diverge at index 3; the claim had already reached the committed audit and was corrected there), and reuse of the second-person SMS refusal string on a screen where the operator is acting on someone else. A `replace_all` of mine matched only one of two call sites because the two differed in indentation — caught by the Reviewer, not by the green build.
Open items:         Next wave, in order: atomic terminal-state resolution to close the remaining duplicate-send window; the drain thread's missing `catch` **together with** moving `resetOrphanedSending()` inside the DRAINING try/finally (doing the first alone converts a loud crash into a permanent silent stall); newline sanitisation + nickname validation + the Unicode-digit fix in `normalize`; re-denominating the daily cap in segments and moving the check into `enqueue`. Still unresolved and needing the owner: **no release signing config** — the installed build is signed by one machine's debug keystore, so losing that machine means no in-place upgrade is possible and the only recovery deletes the member database. **Six releases (5.0–5.5) remain unverified on hardware.**

---

## On-device checklist before the live ~100-member group

Nothing in CI substitutes for these. Items 1 and 2 are the feature's own happy path; 3–8 are the failure modes found in review.

> **RESULTS SO FAR — 2026-09-23, Samsung SM-F711U (Galaxy Z Flip 3), Android 15 / API 35.**
> First hardware verification in this project's history. jRelay 5.6 installed clean over ADB.
>
> - **Item 1 PASSED — a Hebrew message arrived, on a flip phone.** This is the headline: the
>   UCS-2 segment bug silently dropped Hebrew and Yiddish messages of roughly 71-160 characters
>   from v1.0 through v5.3, while recording them as delivered. It is fixed, on real hardware,
>   on the device class the deployment exists for.
> - **Delivery results work end-to-end.** logcat shows 7 outbound sends, every one returning
>   `statusCode 202` from the IMS stack, and 7 matching `com.sh7411usa.jrelay.SMS_SENT`
>   broadcasts back to `SentReceiver`, each with its own `jrelay://sent/...` data URI. One
>   result per send, none lost. Everything in 5.3/5.4/5.5 rests on this mechanism and it had
>   never been exercised on a phone.
> - **The foreground service starts and is permitted on Android 15** —
>   `Background started FGS: Allowed ... targetSdkVersion:36`. The `specialUse` declaration is
>   accepted.
> - **No crashes, no exceptions** across the session.
> - **The 5.5 security fix holds on device:** `run-as: package not debuggable`. Note the
>   consequence — the database can no longer be inspected over ADB, so device checks now rely on
>   logcat plus what recipients actually see.
> - Host setup applied over ADB: permissions pre-granted (sidestepping the broken deny path),
>   Doze exemption whitelisted, and Android's own outgoing-SMS throttle raised from its unset
>   default to 200/30min. That default is ~30 per 30 minutes — roughly 36x BELOW the app's
>   designed sending rate, so it would have tripped on the first real fan-out.
>
> Still unverified: everything below except item 1.

1. **A ~100-character Hebrew message, with the zero-width-space salt ON.** Must arrive as a single message, row marked SENT. **Run this one first.** It is the first direct evidence the app will ever have had about whether these were being delivered at all — before 5.3 a 71-160 character Hebrew or Yiddish message was sent as one part, could not be encoded, never arrived, and was recorded as SENT. If it now arrives, that confirms silent loss that predates every phase in this log.
2. **Airplane mode on the host, then send to a member.** The row must go FAILED with "Phone had no signal (airplane mode or radio off)" — not SENT. This is the basic proof that delivery results are being read at all.
3. **Kill the app process mid-fan-out, reopen, and confirm no member receives a duplicate.** Exercises the PendingIntent identity fix: before it, a restart could tangle two messages onto one callback.
4. **Force a failure on the very last message of a fan-out** (e.g. one member's number unreachable) and confirm the retry goes out **without** an inbound message arriving first. Exercises the drain staying up to collect late results; failures cluster at exactly the moment this matters.
5. **Three posts inside one window, ~100 members** — each member receives ONE text containing all three, in order, salted once. This is the primary acceptance test for the phase.
6. **One inbound SMS, then check logcat for `E/SmsSendService: Could not start send service`.** 5.2 guards the service start so an OEM that ignores the SMS foreground-service allowlist can no longer crash the relay on every incoming message — but that means the failure no longer announces itself. If that line appears, the allowlist does not hold on this device and **5.2 must not go live** until it is resolved. Also confirm the sending notification appears.
7. **A post made during an active fan-out** — post A to ~100 members, let it drain partway, then post B. Verify all three of these, in this order of importance:
   - **Every** member receives B's content. Merging must never cost a recipient — it changes packaging only.
   - Members already served A receive B as a **separate** text.
   - Members still queued receive **one** text containing A then B, in that order.
   This is the case that matters most in practice, because replies arrive while the previous post is still going out. Check the queue count reaches 0 afterwards with nothing stuck in `SENDING`.
8. **A command during a hold** — text `HELP` while posts are held; the reply must arrive promptly rather than waiting out the window.
9. **Two posts during one drain** — the service stays alive and the second post's messages go out.
10. **Full fan-out with the screen off, unplugged, and time it.** A foreground service stops the send being killed; it does not stop the SoC suspending, so the waits between bursts can stretch. Compare wall-clock against what your pacing predicts (~3 minutes at the defaults for 100 members). **Material drift means a `PARTIAL_WAKE_LOCK` is required before the live group**, not optional. Also watch logcat for `IllegalStateException` and for the service being stopped mid-drain, and confirm `adb shell dumpsys activity services com.sh7411usa.jrelay` shows `isForeground=true` while it runs.
11. **Reboot with a non-empty outbox** — confirm `BootReceiver` fires on the Samsung, the queue resumes, and nobody receives a duplicate. Do one reboot while **Paused** too, confirming the repair branch runs and the queue count is right afterwards. Remember the relay only resumes after the phone has been unlocked once — the database is encrypted until then.
12. **Full fan-out with the app backgrounded** — watch logcat for `IllegalStateException` and for the service being stopped mid-drain. Closest thing to real operating conditions.
13. **Upgrade in place from the installed 5.0 build with messages already queued** — the backlog drains and those bodies go out unchanged. The 4→5 migration has never run against a real database.
14. **Pause pressed mid-fan-out** — sending stops within one burst.
15. **Coalesce window set to 0** — one post behaves as 5.0 did, allowing for the three documented deviations (per-recipient salting, pause honoured mid-drain, single-drainer guard).
16. **A post crossing the segment cap** — recipients get two texts, nothing lost or duplicated.
17. **Retry backoff actually delays.** Force a failure (airplane mode as in 2, or an unreachable number), then watch the outbox row: it must stay `PENDING` and be **skipped** by the next burst rather than resent on it, and go out only once its delay has elapsed (30s for a generic failure — use that one, not the 5-minute case, for the first check). Nothing in the test suite covers this, and it is the entire point of 5.4: the tests only pin the pure function that picks the number, not that the queue honours it.
18. **`#help` in each of the three modes, plus while muted.** Four texts, four different replies — and the muted one must say you are muted rather than giving mode advice. Read the Hebrew and Yiddish versions on the device too: the reply-mode help is long enough to become a 3-part SMS at 70 characters/segment once the salt forces UCS-2, so check it arrives whole and reads correctly rather than just that it arrives.
19. **The dashboard during a backoff, with coalescing turned off.** Set the coalesce window to 0, force a failure, and confirm the dashboard reads "Holding 1" — not "Holding 1 to merge". This is the 5.4 wording fix; it is worth one look because that screen is where the host will go to find out why a message hasn't moved.

After 5, 7 and 10, confirm the dashboard queue count returns to 0 and no rows are left in `SENDING` — the cheapest check for the stranded-row risk under real conditions.

**If a run of messages fails with "Sending too fast":** that is almost certainly Android's own per-app throttle, not the carrier, and the backoff in 5.4 will not rescue it — the window is 30 minutes and the retry is 5. Raise the limit via the `adb` command in Settings before concluding anything about the pacing settings or the carrier. Fixing this properly (holding the whole queue, not one message) is 5.5.
