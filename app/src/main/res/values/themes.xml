<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.JRelay" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/primary</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:textColorHint">@color/text_hint</item>
        <item name="android:textColorLink">@color/primary</item>
        <item name="android:textColorHighlight">@color/focus_fill</item>
        <item name="android:colorControlNormal">@color/text_secondary</item>
        <item name="android:colorControlActivated">@color/primary</item>
        <item name="android:colorControlHighlight">@color/pressed_fill</item>
        <!-- Status bar matches the flat surface header -->
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:buttonStyle">@style/Widget.JRelay.Button</item>
        <item name="android:editTextStyle">@style/Widget.JRelay.EditText</item>
        <item name="android:editTextColor">@color/text_primary</item>
    </style>
</resources>
