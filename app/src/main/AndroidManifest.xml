<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Needed ONLY to read inbound group-MMS replies out of content://mms (see
         sms/mms/MmsReader). When a sub-group is a real group thread, a member's reply goes
         peer-to-peer to the whole thread; jRelay is a participant but only learns what was said
         by reading the system MMS store. Without this the other sub-groups never hear it and the
         group silently fragments.

         Reading content://mms requires READ_SMS but is NOT reserved to the default SMS app, so
         jRelay can stay a non-default app. Note this permission must also be REQUESTED at runtime
         before inbound ingestion can work - declaring it is necessary but not sufficient, and the
         natural moment to ask is when the owner switches delivery mode to group threads, since
         that is the only mode where it is used. -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- Lets WapPushReceiver be notified the moment an MMS notification arrives, so
         MmsIngestService can be woken up to scan once the stock app finishes downloading it. -->
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.JRelay">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ConsentActivity" android:exported="false" />
        <activity android:name=".MembershipActivity" android:exported="false" />
        <activity android:name=".MemberDetailActivity" android:exported="false" />
        <activity android:name=".AddMemberActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".SendGroupMessageActivity" android:exported="false" />
        <activity android:name=".LicenseActivity" android:exported="false" />

        <receiver
            android:name=".sms.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".sms.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".sms.SentReceiver"
            android:exported="false" />

        <receiver
            android:name=".sms.mms.MmsSentReceiver"
            android:exported="false" />

        <receiver
            android:name=".sms.mms.WapPushReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_WAP_PUSH">
            <intent-filter>
                <action android:name="android.provider.Telephony.WAP_PUSH_RECEIVED" />
                <data android:mimeType="application/vnd.wap.mms-message" />
            </intent-filter>
        </receiver>

        <service
            android:name=".sms.SmsSendService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Relays SMS to group members on a paced schedule; the send must continue while the screen is off." />
        </service>

        <service
            android:name=".sms.mms.MmsIngestService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Watches for inbound group MMS replies so they can be relayed to the other sub-groups; must continue while the screen is off." />
        </service>

        <provider
            android:name=".sms.mms.MmsFileProvider"
            android:authorities="com.sh7411usa.jrelay.mmsfileprovider"
            android:exported="false"
            android:grantUriPermissions="true" />

    </application>

</manifest>
